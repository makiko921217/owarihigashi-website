# data/

`content.json` は管理画面 (`/admin`) が書き換えるファイルです。

- **サイト本体**はこのファイルをビルド時に読み込んで表示します
- **管理画面**は GitHub API 経由でこのファイルを読み書きします

手で編集しても構いませんが、管理画面を開いている最中に書き換えると
上書き合戦になるので注意してください（管理画面側は更新前に SHA を照合し、
他所で変更されていた場合は保存を中止します）。
